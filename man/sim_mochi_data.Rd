% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_mochi_data.R
\name{sim_mochi_data}
\alias{sim_mochi_data}
\title{Simulate data to test moment-based measurement error correction for the concentration index}
\usage{
sim_mochi_data(
  var_error = 1,
  var_exposure = 0.6,
  corr_exposure_error = 0.9,
  approx_disparity = -0.5,
  n = 1000,
  val_prop = 0.1
)
}
\arguments{
\item{var_error}{scalar for the variance of the errors in the exposure. Default is \code{var_error = 1}.}

\item{var_exposure}{scalar for the variance of the exposure. Default is \code{var_exposure = 0.6}.}

\item{corr_exposure_error}{scalar for the correlation between the exposure and its measurement errors. Default is \code{corr_exposure_error = 0.9}.}

\item{approx_disparity}{scalar for the approximate concentration index underlying the error-free exposure and outcome. Default is \code{approx_disparity = -0.5}.}

\item{n}{scalar for total sample size generated. Default is \code{n = 1000}.}

\item{val_prop}{scalar for proportion of \code{n} to be sampled for the validation study (via simple random sampling). Default is \code{val_prop = 0.1}.}
}
\value{
a dataframe
}
\description{
This function returns a dataframe with all columns needed to apply the \code{mochi} function.
}
